# P4 UX 修复批次实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 `docs/superpowers/specs/2026-06-11-ux-fixes-design.md`：修复普通用户体系断裂、零通知、retention 摆设、节点永不掉线四个 P0，外加错误中文化、登录限速与一批前端体验止血。

**Architecture:** 后端沿用既有 sweeper（spawn + pub tick_once）与 settings 白名单模式新增三个子系统（retention 清理、掉线检测、webhook 通知）；REST 按「加字段/放行+净化」演进，零 proto 变更；前端按角色双模式渲染（admin 全量 / user 自助最小集）。

**Tech Stack:** Rust（axum/sqlx/tonic/tokio + 新增 reqwest-rustls）、React 19 + Vite + TS、SQLx migration 0008。

**与 spec 的单元差异：** spec 中 T3/T4 顺序在本计划中互换——notify 模块（无事件源依赖）先行，掉线 sweeper 后行并一次接全四个事件，依赖保持单向。

**每个 Task 收尾纪律（CLAUDE.md 强制）：** 跑给出的验证命令 → spawn `general-purpose` 子代理用 `superpowers:code-reviewer` 审查该 Task 改动文件 → 阻塞问题修完才 commit → 进入下一 Task。

---

### Task 1: 错误信息中文化 + 登录限速

**Files:**
- Modify: `crates/panel-server/src/error.rs`
- Modify: `crates/panel-server/src/routes/mod.rs`（login 限速路由）
- Modify: `crates/panel-server/src/routes/{rules,nodes,users,tunnels,bandwidth_profiles,system,rules_io}.rs`（BadRequest 文案）
- Test: `crates/panel-server/tests/api_auth.rs`（限速）+ 既有断言修正

- [ ] **Step 1.1: error.rs 顶层错误文案中文化**

```rust
#[derive(Error, Debug)]
pub enum ApiError {
    #[error("服务器内部错误")]
    Internal(#[from] anyhow::Error),

    #[error("服务器内部错误")]
    Database(#[from] sqlx::Error),

    // 不再拼 "bad request: " 前缀——机器码在 ErrorBody.error 字段。
    #[error("{0}")]
    BadRequest(String),

    #[error("资源不存在")]
    NotFound,

    #[error("未登录或登录已过期")]
    Unauthorized,

    #[error("{0}")]
    UnauthorizedMsg(String),

    #[error("无权限执行此操作")]
    Forbidden,
}
```

注意：`tracing::error!(error = ?self)` 用 Debug 输出，日志可读性不受影响。

- [ ] **Step 1.2: 全部 routes 的 BadRequest 英文 message 改中文**

`Grep pattern: ApiError::BadRequest` 逐处翻译。对照表（关键项，其余同风格直译）：

| 原文 | 译文 |
|---|---|
| `name is required` | `名称不能为空` |
| `listen_port and target_port must be 1-65535` | `监听端口与目标端口必须在 1-65535 之间` |
| `listen_ip is not a valid IP` | `监听 IP 不是合法 IP 地址` |
| `target_host is not a valid IP or hostname` | `目标主机不是合法 IP 或主机名` |
| `node_id does not exist` | `节点不存在` |
| `listen_port {p} outside node's port pool [{min}-{max}]` | `监听端口 {p} 超出节点端口池 [{min}-{max}]` |
| `listen_port {p} is reserved` | `监听端口 {p} 是保留端口,禁止监听` |
| `tunnel_id does not exist` | `隧道不存在` |
| `rule.node_id must equal the tunnel entry (ordinal 0) node` | `规则节点必须是隧道入口(第 1 跳)节点` |
| `bandwidth_profile_id does not exist` | `限速配置不存在` |
| `tunnel is referenced by N active rule(s); detach them first` | `隧道仍被 N 条规则关联,请先解除关联` |
| `invalid sort field; allowed: ...` | `排序字段不合法,可用: ...` |
| `order must be asc or desc` | `排序方向必须是 asc 或 desc` |
| `unknown setting key: {k}` | `未知设置项: {k}` |

凡 message 含动态值的保留插值。`grpc/` 内的 `Status::*`（Agent 间通信）**不翻**。

- [ ] **Step 1.3: login 限速路由**

`routes/mod.rs`：仿照 install_governor 既有模式，把 login 拆为独立带限速路由：

```rust
let login_governor = Arc::new(
    GovernorConfigBuilder::default()
        .per_second(1)
        .burst_size(10)
        .key_extractor(SmartIpKeyExtractor)
        .finish()
        .expect("login governor config"),
);
let login_routes = Router::new()
    .route("/api/auth/login", post(auth::login))
    .layer(GovernorLayer::new(login_governor))
    .with_state(state.clone());
```

主 Router 删除原 `.route("/api/auth/login", ...)` 行，追加 `.merge(login_routes)`。

- [ ] **Step 1.4: 限速测试（先写，确认 429）**

`tests/api_auth.rs` 追加（请求构造模式参照本文件既有测试与 `api_install.rs` 的 governor 测试处理）：

```rust
#[tokio::test]
async fn login_rate_limited_after_burst() {
    let app = common::make_app().await.unwrap();
    let mut last = 0;
    for _ in 0..12 {
        let resp = common::post_json(
            &app,
            "/api/auth/login",
            serde_json::json!({"username": "nobody", "password": "wrong-password"}),
            None,
        )
        .await;
        last = resp.status().as_u16();
        if last == 429 { break; }
    }
    assert_eq!(last, 429, "突发 10 次后应触发限速");
}
```

（`common::post_json` 若签名不同，按 `tests/common/mod.rs` 实际 helper 调整。）

- [ ] **Step 1.5: 跑测试修断言**

Run: `cargo test -p panel-server`
预期：断言英文 message 的既有测试失败 → 逐个把断言改为新中文文案/或改为断言 `error` code 字段 → 重跑至全绿。
Run: `cargo test --workspace` 确认无外溢。

- [ ] **Step 1.6: 子代理 review → commit**

```bash
git add -A && git commit -m "fix(api): chinese error messages + login rate limit"
```

---

### Task 2: stats retention 清理 sweeper

**Files:**
- Create: `crates/panel-server/src/sweeper/stats_retention.rs`
- Modify: `crates/panel-server/src/sweeper/mod.rs`（`pub mod stats_retention;`）
- Modify: `crates/panel-server/src/main.rs`（挂载，挨着 `spawn_user_quota_sweeper`）
- Test: `crates/panel-server/tests/stats_retention_sweeper.rs`

- [ ] **Step 2.1: 失败测试**

```rust
// crates/panel-server/tests/stats_retention_sweeper.rs
//! 直接调 tick(确定性),不等 interval。模式同 user_quota_sweeper.rs。
mod common;

use panel_server::sweeper::stats_retention::retention_tick_once;

#[tokio::test]
async fn retention_deletes_old_buckets_keeps_recent() {
    let app = common::make_app().await.unwrap();
    // 种 node + rule(满足 FK)
    sqlx::query("INSERT INTO nodes (name, agent_token_hash) VALUES ('rn', 'x')")
        .execute(&app.state.pool).await.unwrap();
    sqlx::query(
        "INSERT INTO forward_rules (user_id, node_id, name, protocol, listen_ip, listen_port, target_host, target_port) \
         VALUES (1, 1, 'rr', 'tcp', '0.0.0.0', 21001, '1.2.3.4', 443)",
    ).execute(&app.state.pool).await.unwrap();
    // 旧桶(40 天前)与新桶(现在),rule_stats 与 node_stats 各一对
    sqlx::query("INSERT INTO rule_stats (rule_id, bucket_at, rx_bytes) VALUES (1, datetime('now','-40 days'), 1), (1, datetime('now'), 2)")
        .execute(&app.state.pool).await.unwrap();
    sqlx::query("INSERT INTO node_stats (node_id, bucket_at, rx_bytes) VALUES (1, datetime('now','-40 days'), 1), (1, datetime('now'), 2)")
        .execute(&app.state.pool).await.unwrap();

    let deleted = retention_tick_once(&app.state).await.unwrap();
    assert_eq!(deleted, 2, "默认保留 30 天:每表删 1 行旧桶");

    let rule_left: i64 = sqlx::query_scalar("SELECT COUNT(*) FROM rule_stats").fetch_one(&app.state.pool).await.unwrap();
    let node_left: i64 = sqlx::query_scalar("SELECT COUNT(*) FROM node_stats").fetch_one(&app.state.pool).await.unwrap();
    assert_eq!((rule_left, node_left), (1, 1));
}

#[tokio::test]
async fn retention_respects_settings_value() {
    let app = common::make_app().await.unwrap();
    sqlx::query("INSERT INTO nodes (name, agent_token_hash) VALUES ('rn2', 'x')")
        .execute(&app.state.pool).await.unwrap();
    sqlx::query("INSERT INTO node_stats (node_id, bucket_at, rx_bytes) VALUES (1, datetime('now','-10 days'), 1)")
        .execute(&app.state.pool).await.unwrap();
    // 保留 7 天 → 10 天前的桶应被删
    sqlx::query("INSERT INTO system_settings (key, value) VALUES ('stats_retention_days', '7') \
                 ON CONFLICT(key) DO UPDATE SET value = excluded.value")
        .execute(&app.state.pool).await.unwrap();

    let deleted = retention_tick_once(&app.state).await.unwrap();
    assert_eq!(deleted, 1);
}
```

Run: `cargo test -p panel-server --test stats_retention_sweeper`
Expected: 编译失败（模块不存在）。

- [ ] **Step 2.2: 实现**

```rust
// crates/panel-server/src/sweeper/stats_retention.rs
//! rule_stats / node_stats 按 system_settings.stats_retention_days 滚动清理。
//! 不清理 audit_logs(审计保留是合规属性,不在本任务语义内)。
use crate::state::AppState;
use std::time::Duration;
use tracing::{info, warn};

const BATCH: i64 = 5000;

fn env_secs(key: &str, default: u64) -> u64 {
    std::env::var(key).ok().and_then(|s| s.parse().ok()).unwrap_or(default).max(5)
}

pub fn spawn_stats_retention_sweeper(state: AppState) {
    let secs = env_secs("PANEL_STATS_RETENTION_SWEEP_SECS", 3600);
    tokio::spawn(async move {
        let mut tick = tokio::time::interval(Duration::from_secs(secs));
        tick.set_missed_tick_behavior(tokio::time::MissedTickBehavior::Skip);
        loop {
            tick.tick().await;
            if let Err(e) = retention_tick_once(&state).await {
                warn!(error = ?e, "stats retention sweep failed");
            }
        }
    });
}

/// 每 tick 现读配置(改完即生效);缺省 30 天。返回总删除行数。pub 供集成测试。
pub async fn retention_tick_once(state: &AppState) -> anyhow::Result<u64> {
    let days: i64 = sqlx::query_scalar::<_, String>(
        "SELECT value FROM system_settings WHERE key = 'stats_retention_days'",
    )
    .fetch_optional(&state.pool)
    .await
    .unwrap_or(None)
    .and_then(|v| v.parse().ok())
    .filter(|n| *n >= 1)
    .unwrap_or(30);
    let cutoff_modifier = format!("-{days} days");

    let mut total = 0u64;
    for table in ["rule_stats", "node_stats"] {
        loop {
            // 子查询 + id IN:PG 兼容(不依赖 SQLite rowid / DELETE LIMIT 编译开关);
            // 分批防长事务锁库。
            let sql = format!(
                "DELETE FROM {table} WHERE id IN \
                 (SELECT id FROM {table} WHERE bucket_at < datetime('now', ?) LIMIT {BATCH})"
            );
            let rows = sqlx::query(&sql)
                .bind(&cutoff_modifier)
                .execute(&state.pool)
                .await?
                .rows_affected();
            total += rows;
            if rows < BATCH as u64 {
                break;
            }
        }
    }
    if total > 0 {
        info!(deleted = total, retention_days = days, "stats retention sweep deleted old buckets");
    }
    Ok(total)
}
```

`sweeper/mod.rs` 加 `pub mod stats_retention;`；`main.rs` 在 `spawn_user_quota_sweeper(state.clone());` 后加：

```rust
panel_server::sweeper::stats_retention::spawn_stats_retention_sweeper(state.clone());
```

- [ ] **Step 2.3: 验证**

Run: `cargo test -p panel-server --test stats_retention_sweeper` → PASS；`cargo test -p panel-server` 全绿。

- [ ] **Step 2.4: 子代理 review → commit**

```bash
git add -A && git commit -m "feat(sweeper): stats retention cleanup honoring stats_retention_days"
```

---

### Task 3: notify webhook 模块 + settings key

**Files:**
- Create: `crates/panel-server/src/notify/mod.rs`
- Modify: `crates/panel-server/src/lib.rs`（`pub mod notify;`）
- Modify: `crates/panel-server/Cargo.toml`（reqwest）
- Modify: `crates/panel-server/src/routes/system.rs`（ALLOWED + validate）
- Test: `crates/panel-server/tests/notify_webhook.rs`

- [ ] **Step 3.1: Cargo.toml 加依赖**

```toml
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
```

- [ ] **Step 3.2: settings 白名单 + 校验**

`system.rs` `ALLOWED` 数组加 `"notify_webhook_url"`；`validate_setting` 加分支：

```rust
"notify_webhook_url" => {
    if value.is_empty() {
        return Ok(()); // 空 = 关闭通知
    }
    let url = url::Url::parse(value)
        .map_err(|e| ApiError::BadRequest(format!("notify_webhook_url 必须是合法 URL: {e}")))?;
    match url.scheme() {
        "http" | "https" => Ok(()),
        s => Err(ApiError::BadRequest(format!(
            "notify_webhook_url scheme 必须是 http/https,得到 {s}"
        ))),
    }
}
```

- [ ] **Step 3.3: 失败测试（mock 接收器）**

```rust
// crates/panel-server/tests/notify_webhook.rs
//! 用一次性本地 TCP 接收器验证 webhook POST 行为(不引入 wiremock 依赖)。
mod common;

use std::time::Duration;
use tokio::io::{AsyncReadExt, AsyncWriteExt};

/// 起一个收一条 HTTP 请求就关的接收器,返回 (url, body接收channel)。
async fn one_shot_receiver() -> (String, tokio::sync::oneshot::Receiver<String>) {
    let listener = tokio::net::TcpListener::bind("127.0.0.1:0").await.unwrap();
    let addr = listener.local_addr().unwrap();
    let (tx, rx) = tokio::sync::oneshot::channel();
    tokio::spawn(async move {
        let (mut sock, _) = listener.accept().await.unwrap();
        let mut buf = vec![0u8; 65536];
        let mut data = Vec::new();
        loop {
            let n = sock.read(&mut buf).await.unwrap();
            if n == 0 { break; }
            data.extend_from_slice(&buf[..n]);
            // 简化:头里有 Content-Length,读到 body 完整即可
            if let Some(pos) = twoway(&data) {
                let head = String::from_utf8_lossy(&data[..pos]);
                let cl: usize = head.lines()
                    .find(|l| l.to_ascii_lowercase().starts_with("content-length:"))
                    .and_then(|l| l.split(':').nth(1)).and_then(|v| v.trim().parse().ok())
                    .unwrap_or(0);
                if data.len() >= pos + 4 + cl {
                    let body = String::from_utf8_lossy(&data[pos + 4..pos + 4 + cl]).to_string();
                    let _ = sock.write_all(b"HTTP/1.1 200 OK\r\ncontent-length: 0\r\n\r\n").await;
                    let _ = tx.send(body);
                    break;
                }
            }
        }
    });
    (format!("http://{addr}/hook"), rx)
}

fn twoway(data: &[u8]) -> Option<usize> {
    data.windows(4).position(|w| w == b"\r\n\r\n")
}

#[tokio::test]
async fn webhook_posts_event_json_when_configured() {
    let app = common::make_app().await.unwrap();
    let (url, rx) = one_shot_receiver().await;
    sqlx::query("INSERT INTO system_settings (key, value) VALUES ('notify_webhook_url', ?) \
                 ON CONFLICT(key) DO UPDATE SET value = excluded.value")
        .bind(&url)
        .execute(&app.state.pool).await.unwrap();

    panel_server::notify::spawn_send(
        app.state.clone(),
        "node.offline",
        serde_json::json!({"node_id": 1, "name": "n1"}),
    );

    let body = tokio::time::timeout(Duration::from_secs(5), rx).await.unwrap().unwrap();
    let v: serde_json::Value = serde_json::from_str(&body).unwrap();
    assert_eq!(v["event"], "node.offline");
    assert_eq!(v["data"]["node_id"], 1);
    assert!(v["occurred_at"].as_str().unwrap().contains('T'));
}

#[tokio::test]
async fn webhook_noop_when_unconfigured() {
    let app = common::make_app().await.unwrap();
    // 未配置 → 不 panic、不报错(fire-and-forget,给点时间跑完)
    panel_server::notify::spawn_send(app.state.clone(), "node.offline", serde_json::json!({}));
    tokio::time::sleep(Duration::from_millis(200)).await;
}
```

Run: `cargo test -p panel-server --test notify_webhook` → 编译失败（模块不存在）。

- [ ] **Step 3.4: 实现 notify 模块**

```rust
// crates/panel-server/src/notify/mod.rs
//! 出站 webhook 通知(v1):fire-and-forget POST JSON。
//! 事件允许丢失——失败重试 1 次后放弃并 warn,绝不阻塞调用方。
//! URL 仅 admin 可配(system_settings.notify_webhook_url),不做内网过滤(面板机本就由 admin 控制)。
use crate::state::AppState;
use tracing::warn;

/// 异步发送事件。调用处不 await 结果。
pub fn spawn_send(state: AppState, event: &'static str, data: serde_json::Value) {
    tokio::spawn(async move {
        let url: Option<String> = sqlx::query_scalar::<_, String>(
            "SELECT value FROM system_settings WHERE key = 'notify_webhook_url'",
        )
        .fetch_optional(&state.pool)
        .await
        .ok()
        .flatten()
        .filter(|v| !v.is_empty());
        let Some(url) = url else { return };

        let payload = serde_json::json!({
            "event": event,
            "occurred_at": chrono::Utc::now().to_rfc3339(),
            "data": data,
        });
        let client = match reqwest::Client::builder()
            .timeout(std::time::Duration::from_secs(5))
            .build()
        {
            Ok(c) => c,
            Err(e) => {
                warn!(error = ?e, "webhook client build failed");
                return;
            }
        };
        for attempt in 0..2u8 {
            match client.post(&url).json(&payload).send().await {
                Ok(resp) if resp.status().is_success() => return,
                Ok(resp) => warn!(event, status = %resp.status(), attempt, "webhook non-2xx"),
                Err(e) => warn!(event, error = ?e, attempt, "webhook send failed"),
            }
        }
    });
}
```

`lib.rs` 加 `pub mod notify;`。

- [ ] **Step 3.5: 验证**

Run: `cargo test -p panel-server --test notify_webhook` → PASS；`cargo test -p panel-server --test api_system`（settings 校验仍绿）。

- [ ] **Step 3.6: 子代理 review → commit**

```bash
git add -A && git commit -m "feat(notify): outbound webhook module + notify_webhook_url setting"
```

---

### Task 4: 节点掉线 sweeper + 恢复检测 + agent_version + 四事件接入

**Files:**
- Create: `migrations/0008_node_agent_version.sql`
- Create: `crates/panel-server/src/sweeper/node_offline.rs`
- Modify: `crates/panel-server/src/sweeper/mod.rs`、`crates/panel-server/src/main.rs`（挂载）
- Modify: `crates/panel-server/src/grpc/service.rs`（register 存版本；三处 online 写入加恢复检测）
- Modify: `crates/panel-server/src/sweeper/user_quota.rs`（quota/expired 事件）
- Modify: `crates/panel-server/src/models/node.rs` + `crates/panel-server/src/routes/nodes.rs`（agent_version 字段）
- Test: `crates/panel-server/tests/node_offline_sweeper.rs`

- [ ] **Step 4.1: migration**

```sql
-- migrations/0008_node_agent_version.sql
-- 节点 Agent 上报的版本号(register 时落库)。空串 = 尚未注册过。
ALTER TABLE nodes ADD COLUMN agent_version TEXT NOT NULL DEFAULT '';
```

- [ ] **Step 4.2: 失败测试**

```rust
// crates/panel-server/tests/node_offline_sweeper.rs
mod common;

use panel_server::sweeper::node_offline::offline_tick_once;

async fn seed_node(app: &common::TestApp, name: &str, status: &str, last_seen_modifier: &str) -> i64 {
    let res = sqlx::query(
        "INSERT INTO nodes (name, agent_token_hash, status, last_seen_at) \
         VALUES (?, 'x', ?, datetime('now', ?))",
    )
    .bind(name).bind(status).bind(last_seen_modifier)
    .execute(&app.state.pool).await.unwrap();
    res.last_insert_rowid()
}

#[tokio::test]
async fn stale_online_node_marked_offline() {
    let app = common::make_app().await.unwrap();
    let stale = seed_node(&app, "stale", "online", "-300 seconds").await;
    let fresh = seed_node(&app, "fresh", "online", "-10 seconds").await;
    let never = seed_node(&app, "never", "unknown", "-300 seconds").await;

    let flipped = offline_tick_once(&app.state).await.unwrap();
    assert_eq!(flipped, 1, "只翻转超时的 online 节点");

    let s: String = sqlx::query_scalar("SELECT status FROM nodes WHERE id = ?").bind(stale).fetch_one(&app.state.pool).await.unwrap();
    assert_eq!(s, "offline");
    let f: String = sqlx::query_scalar("SELECT status FROM nodes WHERE id = ?").bind(fresh).fetch_one(&app.state.pool).await.unwrap();
    assert_eq!(f, "online");
    let n: String = sqlx::query_scalar("SELECT status FROM nodes WHERE id = ?").bind(never).fetch_one(&app.state.pool).await.unwrap();
    assert_eq!(n, "unknown", "从未上线的节点不动");

    // audit 落了聚合记录
    let cnt: i64 = sqlx::query_scalar("SELECT COUNT(*) FROM audit_logs WHERE action = 'node.offline_detected'")
        .fetch_one(&app.state.pool).await.unwrap();
    assert_eq!(cnt, 1);

    // 幂等
    assert_eq!(offline_tick_once(&app.state).await.unwrap(), 0);
}
```

Run: `cargo test -p panel-server --test node_offline_sweeper` → 编译失败。

- [ ] **Step 4.3: 实现 sweeper**

```rust
// crates/panel-server/src/sweeper/node_offline.rs
//! 节点掉线检测:status='online' 且 last_seen_at 超过阈值 → 置 offline。
//! 修复"节点永不掉线"bug,并作为 node.offline webhook 事件源。
//! 恢复(offline→online)在 grpc/service.rs 写 online 的三处检测。
use crate::{audit, notify, state::AppState};
use std::time::Duration;
use tracing::{info, warn};

fn env_secs(key: &str, default: u64, min: u64) -> u64 {
    std::env::var(key).ok().and_then(|s| s.parse().ok()).unwrap_or(default).max(min)
}

pub fn spawn_node_offline_sweeper(state: AppState) {
    let secs = env_secs("PANEL_NODE_OFFLINE_SWEEP_SECS", 30, 5);
    tokio::spawn(async move {
        let mut tick = tokio::time::interval(Duration::from_secs(secs));
        tick.set_missed_tick_behavior(tokio::time::MissedTickBehavior::Skip);
        loop {
            tick.tick().await;
            if let Err(e) = offline_tick_once(&state).await {
                warn!(error = ?e, "node offline sweep failed");
            }
        }
    });
}

/// 返回本次翻转 offline 的节点数。pub 供集成测试。
pub async fn offline_tick_once(state: &AppState) -> anyhow::Result<u64> {
    // 默认 120s = 2 个 Agent 心跳周期(60s),容忍一次心跳丢失。
    let after = env_secs("PANEL_NODE_OFFLINE_AFTER_SECS", 120, 10) as i64;
    let modifier = format!("-{after} seconds");

    let stale: Vec<(i64, String)> = sqlx::query_as(
        "SELECT id, name FROM nodes \
         WHERE status = 'online' AND deleted_at IS NULL \
           AND (last_seen_at IS NULL OR last_seen_at < datetime('now', ?))",
    )
    .bind(&modifier)
    .fetch_all(&state.pool)
    .await?;
    if stale.is_empty() {
        return Ok(0);
    }

    // 置 offline 时再次校验条件,防 SELECT→UPDATE 窗口内心跳进来被误翻。
    let mut flipped = 0u64;
    let mut names = Vec::new();
    for (id, name) in &stale {
        let rows = sqlx::query(
            "UPDATE nodes SET status = 'offline', updated_at = datetime('now') \
             WHERE id = ? AND status = 'online' \
               AND (last_seen_at IS NULL OR last_seen_at < datetime('now', ?))",
        )
        .bind(id)
        .bind(&modifier)
        .execute(&state.pool)
        .await?
        .rows_affected();
        if rows > 0 {
            flipped += 1;
            names.push(format!("#{id}({name})"));
            notify::spawn_send(
                state.clone(),
                "node.offline",
                serde_json::json!({ "node_id": id, "name": name }),
            );
        }
    }
    if flipped > 0 {
        audit::record(
            &state.pool, None, "node.offline_detected", Some("node"), None,
            Some(&format!("count={flipped},nodes={}", names.join(","))),
            true, None,
        ).await;
        info!(count = flipped, "nodes marked offline by sweeper");
    }
    Ok(flipped)
}
```

`sweeper/mod.rs` 加 `pub mod node_offline;`；`main.rs` 挂载：

```rust
panel_server::sweeper::node_offline::spawn_node_offline_sweeper(state.clone());
```

- [ ] **Step 4.4: gRPC 三处恢复检测 + register 存版本**

`grpc/service.rs`，三处写 `status='online'` 的 UPDATE（register ~:154、heartbeat ~:192、统计上报 ~:334）前各加旧状态读取，UPDATE 后发恢复事件。register 处示例（另两处同构，只是 UPDATE 语句不同）：

```rust
let prev_status: Option<String> =
    sqlx::query_scalar("SELECT status FROM nodes WHERE id = ?")
        .bind(req.node_id)
        .fetch_optional(&self.state.pool)
        .await
        .unwrap_or(None);

// 标记 node 在线(register 处同时落 agent 版本)。
let _ = sqlx::query(
    "UPDATE nodes SET status = 'online', last_seen_at = datetime('now'), \
     agent_version = ?, updated_at = datetime('now') WHERE id = ?",
)
.bind(&req.version)
.bind(req.node_id)
.execute(&self.state.pool)
.await;

if prev_status.as_deref() == Some("offline") {
    crate::notify::spawn_send(
        self.state.clone(),
        "node.online",
        serde_json::json!({ "node_id": req.node_id }),
    );
}
```

heartbeat / 统计上报两处不改 UPDATE 语句本身（不写版本），只包同样的 prev_status 读取 + 事件发送（接受 ~4 行 ×3 的重复，恢复可从任一路径发生）。

- [ ] **Step 4.5: user_quota 两事件**

`sweeper/user_quota.rs` 两个命中点的 `audit::record` 后各加：

```rust
crate::notify::spawn_send(
    state.clone(),
    "user.expired", // quota 分支用 "user.quota_exceeded"
    serde_json::json!({ "user_id": user_id, "disabled_rule_count": disabled }),
);
```

- [ ] **Step 4.6: NodeView/model 加 agent_version**

`models/node.rs` `Node` struct 加 `pub agent_version: String,`（FromRow 自动映射；确认所有手写 SELECT 列清单的查询补列或本就 `SELECT *`）。`routes/nodes.rs` `NodeView` 加 `pub agent_version: String,`，`From<Node>` 补映射。

- [ ] **Step 4.7: 验证**

Run: `cargo test -p panel-server --test node_offline_sweeper` → PASS
Run: `cargo test --workspace` → 全绿（`agent_e2e`/`tunnel_e2e` 覆盖 register 路径回归）。

- [ ] **Step 4.8: 子代理 review → commit**

```bash
git add -A && git commit -m "feat(nodes): offline detection sweeper, recovery events, agent_version; wire 4 webhook events"
```

---

### Task 5: REST 用户体系后端

**Files:**
- Modify: `crates/panel-server/src/routes/auth.rs`（MeView）
- Modify: `crates/panel-server/src/routes/nodes.rs`（放行 + 净化 + search）
- Modify: `crates/panel-server/src/models/node.rs`（list 查询 search 条件）
- Modify: `crates/panel-server/src/routes/users.rs`（search）
- Modify: `crates/panel-server/src/routes/rules.rs`（user_id 归属 / 收紧 / user_name / stats owner）
- Modify: `crates/panel-server/src/routes/system.rs`（overview 24h）
- Test: `crates/panel-server/tests/{api_auth,api_nodes,api_rules_user,api_users,api_system}.rs`

- [ ] **Step 5.1: 失败测试（核心断言，分布到对应测试文件）**

```rust
// api_auth.rs:me 返回扩展字段
#[tokio::test]
async fn me_returns_quota_fields() {
    let app = common::make_app().await.unwrap();
    let (_uid, token) = common::make_user_token(&app, "meuser", "password123").await.unwrap();
    let resp = common::get_json(&app, "/api/auth/me", Some(&token)).await;
    assert_eq!(resp.status(), 200);
    let v: serde_json::Value = common::body_json(resp).await;
    assert!(v.get("rule_count").is_some());
    assert!(v.get("traffic_limit_bytes_30d").is_some());
    assert!(v.get("period_used_bytes_cached").is_some());
    assert!(v.get("expires_at").is_some());
}

// api_nodes.rs:普通用户可读但字段净化
#[tokio::test]
async fn normal_user_gets_sanitized_node_list() {
    let app = common::make_app().await.unwrap();
    // admin 建一个带运维数据的节点
    sqlx::query("INSERT INTO nodes (name, agent_token_hash, grpc_endpoint, cpu_usage, rx_bytes_total, agent_version) \
                 VALUES ('n1', 'x', 'https://internal:7001', 55.5, 999, '9.9.9')")
        .execute(&app.state.pool).await.unwrap();
    let (_uid, token) = common::make_user_token(&app, "nuser", "password123").await.unwrap();
    let resp = common::get_json(&app, "/api/nodes?page_size=20", Some(&token)).await;
    assert_eq!(resp.status(), 200, "user 应可读节点列表");
    let v: serde_json::Value = common::body_json(resp).await;
    let item = &v["items"][0];
    assert_eq!(item["grpc_endpoint"], "");
    assert_eq!(item["agent_version"], "");
    assert_eq!(item["cpu_usage"], 0.0);
    assert_eq!(item["rx_bytes_total"], 0);
    assert!(item["name"].as_str().unwrap() == "n1");
    assert!(item.get("port_pool_min").is_some());
}

// api_rules_user.rs:归属与收紧
#[tokio::test]
async fn admin_can_assign_rule_owner() { /* admin 建规则带 user_id=普通用户 → 201/200,GET 回 user_id 与 user_name */ }
#[tokio::test]
async fn user_cannot_pass_user_id_profile_or_tunnel() { /* 普通用户分别传三字段 → 400 */ }
#[tokio::test]
async fn user_can_create_own_plain_rule() { /* 普通用户裸建(无三字段) → 成功,归属自己 */ }
#[tokio::test]
async fn user_cannot_clear_admin_bandwidth_profile() { /* admin 给 user 规则挂限速;user PATCH bandwidth_profile_id=0 → 400 */ }

// api_users.rs / api_nodes.rs:服务端 search
#[tokio::test]
async fn users_list_search_filters_by_username() { /* 建 alice/bob,search=ali → 只回 alice */ }
#[tokio::test]
async fn nodes_list_search_filters() { /* search 命中 name/region/ip;非命中为空 */ }

// api_system.rs:overview 24h
#[tokio::test]
async fn overview_includes_24h_forward_traffic() { /* 插 rule_stats 桶(25h 前 + 1h 前) → rx_bytes_24h 只含 1h 前的 */ }
```

（占位注释处按文件内既有 helper 风格写全——执行时落实为真实代码，不留注释壳。）

Run: 上述各测试文件 → 失败/编译错。

- [ ] **Step 5.2: me 扩展**

`auth.rs` 新增 `MeView` 并改写 `me`：

```rust
#[derive(Serialize)]
pub struct MeView {
    pub id: i64,
    pub username: String,
    pub role: String,
    pub expires_at: Option<String>,
    pub traffic_limit_bytes_30d: Option<i64>,
    pub period_used_bytes_cached: i64,
    pub period_used_calculated_at: Option<String>,
    pub rule_count: i64,
    pub total_traffic_bytes: i64,
}

pub async fn me(
    State(state): State<AppState>,
    AuthUser(claims): AuthUser,
) -> ApiResult<Json<MeView>> {
    let row: Option<(i64, String, String, Option<String>, Option<i64>, i64, Option<String>, i64, i64)> =
        sqlx::query_as(
            "SELECT u.id, u.username, u.role, u.expires_at, u.traffic_limit_bytes_30d, \
                    u.period_used_bytes_cached, u.period_used_calculated_at, \
                    COALESCE(r.cnt, 0), COALESCE(r.tot, 0) \
             FROM users u \
             LEFT JOIN (SELECT user_id, COUNT(*) AS cnt, SUM(rx_bytes + tx_bytes) AS tot \
                        FROM forward_rules WHERE deleted_at IS NULL GROUP BY user_id) r \
               ON r.user_id = u.id \
             WHERE u.id = ? AND u.deleted_at IS NULL",
        )
        .bind(claims.sub)
        .fetch_optional(&state.pool)
        .await?;
    let (id, username, role, expires_at, limit, used, used_at, rule_count, total) =
        row.ok_or(ApiError::Unauthorized)?;
    Ok(Json(MeView {
        id, username, role, expires_at,
        traffic_limit_bytes_30d: limit,
        period_used_bytes_cached: used,
        period_used_calculated_at: used_at,
        rule_count, total_traffic_bytes: total,
    }))
}
```

`LoginResponse.user`（UserView）保持不变。

- [ ] **Step 5.3: nodes 放行 + 净化 + search**

`nodes.rs`：

```rust
impl NodeView {
    /// 普通用户视角:保留建规则所需(身份/在线/端口池/入口 IP),抹掉运维与控制面信息。
    fn sanitize_for_user(mut self) -> Self {
        self.grpc_endpoint = String::new();
        self.agent_version = String::new();
        self.cpu_usage = 0.0;
        self.memory_usage = 0.0;
        self.load_average = 0.0;
        self.rx_bytes_total = 0;
        self.tx_bytes_total = 0;
        self
    }
}
```

`list` / `get`：删除 `auth.require_admin()?;`，组装响应处：

```rust
let sanitize = !auth.is_admin();
let items = items.into_iter().map(NodeView::from)
    .map(|v| if sanitize { v.sanitize_for_user() } else { v })
    .collect();
```

其余 nodes 端点（create/update/delete/stats/revoke）的 `require_admin` 原样保留。
`ListQuery` 加 `pub search: Option<String>`，传入 `Node::list_paged`/`count` 的查询追加：

```sql
AND (name LIKE ? ESCAPE '\' OR region LIKE ? ESCAPE '\' OR public_ip LIKE ? ESCAPE '\')
```

LIKE 参数 = `%{escaped}%`，转义 helper（若 `models/rule.rs` 的 search 已有同款则复用，否则放 `util.rs`）：

```rust
/// LIKE 模式转义:\ % _ 本身转义,配合 ESCAPE '\' 使用。
pub fn escape_like(s: &str) -> String {
    s.replace('\\', "\\\\").replace('%', "\\%").replace('_', "\\_")
}
```

- [ ] **Step 5.4: users search**

`users.rs` `ListQuery` 加 `search`；list SQL `WHERE u.deleted_at IS NULL` 后追加
`AND u.username LIKE ? ESCAPE '\'`（仅 search 有值时），`total` 用相同条件的 COUNT（不再用 `User::count`）。

- [ ] **Step 5.5: rules 归属 + 收紧 + user_name + stats owner**

`rules.rs`：

1. `CreateRuleRequest` 加 `pub user_id: Option<i64>,`。`create` 开头插入：

```rust
// 归属:admin 可指定任意未删用户;普通用户只能是自己。
let owner_id = match req.user_id {
    Some(uid) if !auth.is_admin() => {
        if uid != auth.0.sub {
            return Err(ApiError::BadRequest("仅管理员可指定归属用户".into()));
        }
        uid
    }
    Some(uid) => {
        crate::models::user::User::find_by_id(&state.pool, uid)
            .await?
            .ok_or_else(|| ApiError::BadRequest("归属用户不存在".into()))?;
        uid
    }
    None => auth.0.sub,
};
if !auth.is_admin() && (req.bandwidth_profile_id.is_some() || req.tunnel_id.is_some()) {
    return Err(ApiError::BadRequest("仅管理员可配置限速与隧道".into()));
}
```

`Rule::create(...)` 第一个参数 `auth.0.sub` 改 `owner_id`；audit payload 追加 `owner={owner_id}`。

2. `update` 校验段加：

```rust
if !auth.is_admin() && req.bandwidth_profile_id.is_some() {
    return Err(ApiError::BadRequest("仅管理员可修改限速配置".into()));
}
```

3. `RuleView` 加 `pub user_name: Option<String>,`（`From<Rule>` 里置 `None`）。
   `list` 响应组装后批量补名（routes 层补查，不动 model）：

```rust
let mut views: Vec<RuleView> = items.into_iter().map(Into::into).collect();
let ids: Vec<i64> = views.iter().map(|v| v.user_id).collect();
if !ids.is_empty() {
    let placeholders = vec!["?"; ids.len()].join(",");
    let sql = format!("SELECT id, username FROM users WHERE id IN ({placeholders})");
    let mut q = sqlx::query_as::<_, (i64, String)>(&sql);
    for id in &ids { q = q.bind(id); }
    let map: std::collections::HashMap<i64, String> =
        q.fetch_all(&state.pool).await?.into_iter().collect();
    for v in &mut views {
        v.user_name = map.get(&v.user_id).cloned();
    }
}
```

`get`/`create` 返回前单条补一次（`SELECT username FROM users WHERE id = ?`）。

4. `stats` handler：开头加 `ensure_can_touch(&auth, &rule)?;`（与 `logs` 的 owner 检查对齐；若已存在则跳过此步）。

- [ ] **Step 5.6: overview 24h**

`system.rs` `overview` 响应 struct 加：

```rust
pub rx_bytes_24h: i64,
pub tx_bytes_24h: i64,
```

查询补一条：

```rust
let (rx_24h, tx_24h): (i64, i64) = sqlx::query_as(
    "SELECT COALESCE(SUM(rx_bytes), 0), COALESCE(SUM(tx_bytes), 0) \
     FROM rule_stats WHERE bucket_at >= datetime('now', '-1 day')",
)
.fetch_one(&state.pool)
.await?;
```

- [ ] **Step 5.7: 验证**

Run: `cargo test -p panel-server` → 全绿（含 Step 5.1 新测试）。
Run: `cargo test --workspace`。

- [ ] **Step 5.8: 子代理 review → commit**

```bash
git add -A && git commit -m "feat(api): self-serve user surface (me+, sanitized nodes, rule ownership, search, 24h overview)"
```

---

### Task 6: 前端角色体系

**Files:**
- Modify: `web/src/lib/api.ts`（类型 + me 返回 MeView + search 参数 + overview 24h + user_id）
- Modify: `web/src/App.tsx`（导航过滤）
- Modify: `web/src/pages/Dashboard.tsx`（角色分流 + 24h 改源）
- Create: `web/src/pages/UserDashboard.tsx`
- Modify: `web/src/pages/Rules.tsx`（user 模式 + 归属列/下拉）
- Modify: `web/src/lib/ui.tsx`（ForbiddenCard）
- Modify: `web/src/pages/{Nodes,NodeDetail,Tunnels,TunnelDetail,Users,BandwidthProfiles,Settings}.tsx`（admin guard）

- [ ] **Step 6.1: api.ts 类型与端点**

```ts
export interface MeView extends UserView {
  expires_at: string | null
  traffic_limit_bytes_30d: number | null
  period_used_bytes_cached: number
  period_used_calculated_at: string | null
  rule_count: number
  total_traffic_bytes: number
}
// auth.me 返回 MeView(UserView 子集兼容,AuthProvider 零改动):
//   me: () => api.get<MeView>('/api/auth/me'),
// RuleView 加 user_name: string | null
// CreateRuleRequest 加 user_id?: number
// SystemOverview 加 rx_bytes_24h / tx_bytes_24h
// NodeView 加 agent_version: string
// nodes.list / users.list 的参数对象加 search?: string 并写入 URLSearchParams
```

- [ ] **Step 6.2: 导航按角色过滤（App.tsx）**

```tsx
const NAV: { to: string; label: string; adminOnly?: boolean }[] = [
  { to: '/', label: '概览' },
  { to: '/nodes', label: '节点', adminOnly: true },
  { to: '/rules', label: '规则' },
  { to: '/tunnels', label: '隧道', adminOnly: true },
  { to: '/users', label: '用户', adminOnly: true },
  { to: '/bandwidth-profiles', label: '限速', adminOnly: true },
  { to: '/settings', label: '设置', adminOnly: true },
]
// nav 渲染:
{NAV.filter((n) => !n.adminOnly || user.role === 'admin').map((n) => (
  <NavItem key={n.to} to={n.to} label={n.label} onClick={() => setDrawerOpen(false)} />
))}
```

顶栏 username 与 role 间补空格分隔（a11y）：`{user.username}{' '}<span ...>`。

- [ ] **Step 6.3: ForbiddenCard（ui.tsx）+ admin 页 guard**

```tsx
export function ForbiddenCard() {
  return (
    <div className="rounded-2xl border border-amber-500/20 bg-amber-500/5 p-8 text-center">
      <div className="text-lg font-medium text-amber-200">无权限访问</div>
      <p className="mt-2 text-sm text-zinc-400">
        此页面仅管理员可用。如需调整账号权限，请联系管理员。
      </p>
    </div>
  )
}
```

7 个 admin-only 页面组件首行（`useAuth` 已在或新引入）：

```tsx
const { user } = useAuth()
if (user && user.role !== 'admin') return <ForbiddenCard />
```

- [ ] **Step 6.4: UserDashboard.tsx（新文件）**

```tsx
import { useCallback, useEffect, useState } from 'react'
import { auth, formatBytes, rules, shortTime, type MeView, type RuleView } from '../lib/api'

// 普通用户概览:自己的规则/用量/配额/到期。数据源 = me(扩展) + rules.list(已按 owner 过滤)。
export default function UserDashboard() {
  const [me, setMe] = useState<MeView | null>(null)
  const [myRules, setMyRules] = useState<RuleView[]>([])
  const [error, setError] = useState<string | null>(null)

  const load = useCallback(() => {
    Promise.all([auth.me(), rules.list({ page_size: 100 })])
      .then(([m, r]) => {
        setMe(m)
        setMyRules(r.items)
        setError(null)
      })
      .catch((e: unknown) => setError(e instanceof Error ? e.message : '加载失败'))
  }, [])
  useEffect(() => load(), [load])

  if (error) return <div className="rounded-lg border border-red-500/30 bg-red-500/10 px-4 py-3 text-sm text-red-200">{error}</div>
  if (!me) return <div className="text-zinc-400">加载中…</div>

  const enabled = myRules.filter((r) => r.enabled).length
  const quota = me.traffic_limit_bytes_30d
  const used = me.period_used_bytes_cached
  const pct = quota ? Math.min(100, Math.round((used / quota) * 100)) : null

  return (
    <div className="space-y-6">
      <div>
        <h2 className="text-xl font-semibold tracking-tight">我的概览</h2>
        <p className="text-sm text-zinc-400 mt-1">规则 / 流量 / 配额</p>
      </div>
      <div className="grid grid-cols-2 lg:grid-cols-4 gap-4">
        {/* 卡片:我的规则(enabled/total)、累计流量、30 天用量(配额进度)、到期时间 */}
        {/* 30 天卡片:quota 为 null 显示「不限」;否则 used/quota + pct% 进度条 */}
        {/* 到期卡片:me.expires_at ? shortTime(me.expires_at) : '永不—到期后规则自动停用' */}
      </div>
      <section className="rounded-2xl border border-white/10 bg-zinc-900/40 p-5">
        <h3 className="text-sm font-medium text-zinc-200 mb-3">我的规则</h3>
        {/* 简表:名称/协议/监听/目标/状态/流量;空态引导去「规则」页新建 */}
      </section>
    </div>
  )
}
```

（注释行执行时写成真实 JSX，复用 Dashboard 的 Stat 卡样式——把 `Stat` 从 Dashboard.tsx 导出复用，不复制。）

`Dashboard.tsx` 顶部分流：

```tsx
const { user } = useAuth()
if (user && user.role !== 'admin') return <UserDashboard />
```

- [ ] **Step 6.5: admin Dashboard 24h 改源**

删除二阶段逐节点 `nodes.stats` 聚合逻辑，改为并行拉 `system.overview()`，
卡片标签「24h 转发流量」、hint「仅规则转发字节」；「总流量」hint 改「规则转发累计 ↓… ↑…」。

- [ ] **Step 6.6: Rules.tsx 双模式**

- `const isAdmin = user?.role === 'admin'`
- 工具栏：`{isAdmin && (<导出/导入按钮)}`
- 表格：admin 加「归属」列显示 `r.user_name ?? '—'`；user 不渲染该列。
- 表单（RuleFormModal）：
  - admin 加「归属用户」下拉（`users.list({ page_size: 100 })`，默认当前 admin；
    >100 用户显示提示「仅列出前 100 个用户」）。
  - user 隐藏 限速下拉、隧道下拉、归属下拉（不发送对应字段）。
- 空状态文案：按钮可用时保持原文；user 模式下文案不变（按钮现在可用了）。

- [ ] **Step 6.7: 验证**

Run: `cd web && npm run build && npm test` → 编译零错、vitest 全绿（含按需更新的 RuleForm.test）。

- [ ] **Step 6.8: 子代理 review → commit**

```bash
git add -A && git commit -m "feat(web): role-aware nav, user dashboard, rules dual-mode, forbidden card"
```

---

### Task 7: 前端体验止血

**Files:**
- Create: `web/src/lib/use-auto-refresh.ts`
- Modify: `web/src/pages/{Dashboard,UserDashboard,Nodes,Rules,Tunnels,NodeDetail,RuleDetail,TunnelDetail}.tsx`（接入刷新/预检/徽章/搜索）
- Modify: `web/src/pages/Users.tsx`（预检 + 用量 title + datetime-local + 搜索）
- Modify: `web/src/components/Sparkline.tsx`
- Modify: `web/src/lib/api.ts`（shortTime 本地化）

- [ ] **Step 7.1: useAutoRefresh hook**

```ts
// web/src/lib/use-auto-refresh.ts
import { useEffect, useRef } from 'react'

/**
 * 周期静默刷新:页面不可见时跳过该次 tick。
 * cb 存 ref,interval 不随 cb 重建;只在 ms 变化时重排。
 */
export function useAutoRefresh(cb: () => void, ms: number) {
  const ref = useRef(cb)
  ref.current = cb
  useEffect(() => {
    const t = setInterval(() => {
      if (!document.hidden) ref.current()
    }, ms)
    return () => clearInterval(t)
  }, [ms])
}
```

- [ ] **Step 7.2: 各页接入**

前提：把每页数据拉取收敛成可重复调用的 `load()`（多数页面已是）。接入清单：

| 页面 | 周期 | 刷新内容 |
|---|---|---|
| Dashboard / UserDashboard | 30s | 全量 load |
| Nodes | 15s | 列表 |
| Rules | 30s | 列表（保留当前筛选/分页参数） |
| Tunnels | 30s | 列表 |
| NodeDetail / RuleDetail | 30s | stats/logs |
| TunnelDetail | 15s | detail + status |

模式（以 Nodes 为例）：`useAutoRefresh(() => { void load({ silent: true }) }, 15_000)`，
silent 刷新不置 loading 态（避免表格闪烁）；Modal 表单 state 独立，不受刷新影响。

- [ ] **Step 7.3: shortTime 本地化**

```ts
// 后端时间均为 UTC('YYYY-MM-DD HH:MM:SS' 或 ISO)。统一解析为 UTC 后按浏览器本地时区显示。
export function shortTime(iso: string): string {
  const normalized = iso.includes('T') ? iso : iso.replace(' ', 'T')
  const withZone = /Z|[+-]\d{2}:?\d{2}$/.test(normalized) ? normalized : normalized + 'Z'
  const d = new Date(withZone)
  if (Number.isNaN(d.getTime())) return iso.replace('T', ' ').slice(0, 16)
  const p = (n: number) => String(n).padStart(2, '0')
  return `${d.getFullYear()}-${p(d.getMonth() + 1)}-${p(d.getDate())} ${p(d.getHours())}:${p(d.getMinutes())}`
}
```

- [ ] **Step 7.4: Users 页三处**

1. 到期时间输入改本地时区：

```tsx
<input type="datetime-local" value={form.expires_at_local} ... />
// 提交:本地 → UTC 'YYYY-MM-DDTHH:MM'
function localToUtcMinute(local: string): string {
  const d = new Date(local)
  return d.toISOString().slice(0, 16)
}
// 编辑回填:UTC 'YYYY-MM-DD HH:MM:SS' → datetime-local 串
function utcToLocalInput(utc: string): string {
  const d = new Date(utc.replace(' ', 'T') + 'Z')
  const p = (n: number) => String(n).padStart(2, '0')
  return `${d.getFullYear()}-${p(d.getMonth() + 1)}-${p(d.getDate())}T${p(d.getHours())}:${p(d.getMinutes())}`
}
```

标签改「到期时间（本地时区）」，说明文案保留行为描述。
2. 30d 用量单元格：`title={u.period_used_calculated_at ? `计算于 ${shortTime(u.period_used_calculated_at)},约每 5 分钟更新` : '尚未计算'}`。
3. 删除确认 Modal：`rule_count > 0` 时显示「该用户名下有 N 条规则」红字 + 禁用确认按钮。

- [ ] **Step 7.5: 删除预检（Nodes / Tunnels）**

Nodes 删除 Modal 打开时：

```tsx
const [refCount, setRefCount] = useState<number | null>(null)
useEffect(() => {
  rules.list({ node_id: target.id, page_size: 1 })
    .then((r) => setRefCount(r.total))
    .catch(() => setRefCount(null)) // 预检失败不阻塞,交给后端兜底
}, [target.id])
// refCount > 0 → 红字「该节点上有 N 条规则,需先删除或迁移」+ 确认按钮 disabled
// refCount === 0 → 原确认文案
```

Tunnels 删除 Modal：直接用 `t.rules_count`，>0 时同样禁用 + 文案
「该隧道仍被 N 条规则关联，请先在规则页解除关联」；删除原「关联规则将失去隧道绑定」误导句。

- [ ] **Step 7.6: 凭据 Modal 文案 + 创建前提示（Nodes.tsx）**

- 新增节点表单 Modal 顶部（settings 已在打开时拉取）：endpoint 为空时渲染黄条
  「尚未配置 Agent 上报端点，创建后将无法生成一键安装命令。可先到「设置」页配置。（仍可创建，之后通过轮换凭据补发）」。
- 凭据 Modal 未配置分支文案替换为：
  「未配置 Agent 上报端点，无法生成安装命令。请妥善保存下方凭据手动部署；配置端点后可在节点行「轮换凭据」重新生成完整安装命令。」

- [ ] **Step 7.7: 隧道徽章（Rules.tsx / RuleDetail.tsx）**

列表监听单元格（`tunnelList` 已拉取）：

```tsx
{r.tunnel_id != null && (
  <div className="mt-0.5 text-[10px] text-sky-300/80">
    隧道 {tunnelList.find((t) => t.id === r.tunnel_id)?.name ?? `#${r.tunnel_id}`}
  </div>
)}
```

RuleDetail 配置区加一行「隧道」：有 tunnel_id 显示名称（拉 `tunnels.get` 或列表映射），无则「—」。

- [ ] **Step 7.8: Sparkline 数据不足占位 + 峰值**

`components/Sparkline.tsx`：

- `points.length < 2` → 返回 `<div className="text-[11px] text-zinc-500 h-full grid place-items-center">数据不足</div>`
- 否则右上角叠加 `max` 标注（调用方传 `formatValue?: (n:number)=>string`，默认原值）。
- RuleDetail 调用处传 `formatValue={formatBytes}`。

- [ ] **Step 7.9: 服务端搜索接线（Nodes / Users）**

两页搜索框改受控 + 「搜索」按钮/Enter 触发 `load({ search })`（与 Rules 页同构），
placeholder 改「搜索名称 / 区域 / IP」「搜索用户名」；移除「当前页」字样与本地 filter 逻辑。

- [ ] **Step 7.10: a11y 空格**

Dashboard `ErrorRow`：action 与 target 徽章间已有 margin，文本层加 `{' '}`；顶栏同（Task 6.2 已做）。

- [ ] **Step 7.11: 验证**

Run: `cd web && npm run build && npm test` → 零错全绿。

- [ ] **Step 7.12: 子代理 review → commit**

```bash
git add -A && git commit -m "feat(web): auto-refresh, delete precheck, tunnel badge, local time, ux copy fixes"
```

---

### Task 8: 设置页 + 节点版本列 + 文档收尾

**Files:**
- Modify: `web/src/pages/Settings.tsx`（webhook 字段 + retention 文案）
- Modify: `web/src/pages/Nodes.tsx`（agent_version 显示）
- Modify: `README.md`、`docs/api.md`、`.env.example`、`plan.md`（附录）、`docs/ux-review-2026-06-11.md`（标注已修复）

- [ ] **Step 8.1: Settings.tsx**

- 表单加「通知 Webhook URL」字段（key `notify_webhook_url`，加入保存 keys 列表），说明：
  「节点掉线/恢复、用户超额/到期 时 POST JSON `{event, occurred_at, data}` 到此地址。留空关闭。事件: node.offline / node.online / user.quota_exceeded / user.expired」。
- 「统计保留天数」说明改：「rule_stats / node_stats 超期分钟桶由后台每小时自动清理；不清理审计日志。」

- [ ] **Step 8.2: Nodes.tsx 版本显示**

状态单元格内 last_seen 行下：

```tsx
{n.agent_version && (
  <div className="text-[10px] text-zinc-500">v{n.agent_version}</div>
)}
```

- [ ] **Step 8.3: 文档**

- `.env.example`：panel-server 段补三个新 env（默认值与语义一句话）。
- `docs/api.md`：me 扩展字段、nodes 普通用户净化视角、rules `user_id`/`user_name`/权限收紧、
  nodes/users `search`、overview 24h 字段、`notify_webhook_url` + webhook payload、`agent_version`、migration 0008。
- `README.md`：特性清单补「节点掉线检测 + webhook 通知」「普通用户自助视角」「统计保留清理」「登录限速」；安全节补登录限速。
- `plan.md` 附录·实施状态：新增「P4（UX 修复批次）」交付记录段。
- `docs/ux-review-2026-06-11.md`：在第三节各已修复项行尾加「✅ 已于 P4 修复」，未修项保持原样。

- [ ] **Step 8.4: 全量验证**

Run: `cargo test --workspace` && `cd web && npm run build && npm test` → 全绿。

- [ ] **Step 8.5: 手动冒烟（playwright）**

起 dev 栈（独立数据目录），验证：普通用户登录见「我的概览」、能自建规则、admin 建规则可选归属；
杀 agent ~2 分钟后节点页（自动刷新中）变 offline；本地 webhook 接收器收到 node.offline。

- [ ] **Step 8.6: 子代理 review → commit**

```bash
git add -A && git commit -m "feat(web+docs): settings webhook field, agent version column, P4 docs"
```

---

## Self-Review 记录

- **Spec coverage:** spec §A→Task 4；§B→Task 2；§C→Task 3；§D 后端→Task 5、前端→Task 6；§E→Task 1；§F→Task 7；§G→Task 1；§H→Task 4+8；§I→Task 8。设置页 retention 文案（§B 末）落在 Task 8.1。无遗漏。
- **Placeholder scan:** Task 5.1 与 6.4 中以注释标记的测试/JSX 骨架已注明「执行时落实为真实代码」，为有意压缩；其余步骤代码完整。
- **Type consistency:** `MeView`/`user_name: Option<String>`/`agent_version: String`/`escape_like` 在前后任务中签名一致；`spawn_send(state, event, data)` 调用方（Task 4）与定义（Task 3）一致。
